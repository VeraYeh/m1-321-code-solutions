/* exported getSumOfSquares */

function getSumOfSquares(x, y) {
  var squareOfx = x * x;
  var squareOfy = y * y;
  var sumOfSquares = squareOfx + squareOfy;
  return sumOfSquares;
}

getSumOfSquares(4, 5);
